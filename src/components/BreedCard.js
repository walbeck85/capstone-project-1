import React from "react";

// We will eventually pass 'breed' as a prop
function BreedCard() { 
  return (
    <div style={{ border: "1px solid #ccc", padding: "1rem", margin: "0.5rem" }}>
      <h4>Breed Name Placeholder</h4>
      {/* We'll add the Link wrapper and image here */}
    </div>
  );
}

export default BreedCard;